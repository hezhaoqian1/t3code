# Feishu connector resources

The bundled `lark-*` Skills are copied unchanged from `larksuite/cli` tag `v1.0.86`:

- Source: `https://github.com/larksuite/cli`
- Commit: `defd27b9d2f976fe35337b83071d7f11ee0cb1d3`
- License: MIT

The native CLI is provided by the pinned npm dependency `@larksuite/cli@1.0.86`. Its
checksummed postinstall runs on each native release runner so macOS arm64 and Windows x64 packages
contain the matching executable.
